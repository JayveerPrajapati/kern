# Kern Next-Plan Gap Tracker

Source: verification of `docs/audit/sources/kern_next_plan/{plan1.txt, kern_architecture_report.txt}` (vendored 2026-09-03; originals live in the sibling workspace `support_docs/kern_next_plan/`)
(feature proposals + architecture report) against the codebase on 2026-09-02
(HEAD `2443259`, clean tree; build/vet/`go test -short ./...` 87/87 PASS).

Stable IDs: G-1 … G-11. Update this file as work completes; one commit per item.

Status: TODO → IN PROGRESS → DONE → VERIFIED (build+vet+targeted tests+CLI check)
        / DECLINED (with reason).

## Wave 1 — PARTIAL verdicts (work first)

| ID | Gap | Current state | Fix | Status |
|----|-----|---------------|-----|--------|
| G-1 | Live guard-event streaming | Guard events persist to `.kern/events.jsonl` (cmd/kern/cmd_context.go:495 publishGuardEvents) but the relay only replays the file at server start; lock events stream live via relay.Client.Emit | Emit guard events live to `.kern/events.sock` when a relay is running (same pattern as lock contention hook), from both the CLI guard path and the MCP handler | **VERIFIED** — new `relay.PublishPersisted` (dual-leg: durable append to `eventbus.DefaultPersistPath` + live emit through the socket), shared builders `intel.GuardEvent`/`GuardNotConfiguredEvent`/`GuardEvents` with explicit `guard-*` IDs (duplicate-safe via eventbus P4.3 idempotency when a persisting relay owner re-appends); wired into CLI `publishGuardEvents` and MCP `handleGuardCheck` (before the threshold gate, mirroring CLI's persist-before-REJECT). Tests: relay `TestPublishPersisted*` (3), intel `TestGuardEventShapes`/`TestGuardEventsBatch`, mcp `TestGuardCheckPublishesEventsViaMCP`/`...WarningWhenUnconfiguredViaMCP`. Build/vet/targeted pkgs all PASS |
| G-2 | Audit chain coverage | SHA-256 chain (internal/governance) records firewall decisions, approvals, gated exec only — MCP tool executions are NOT recorded | Append an audit entry per MCP tool dispatch (read-only tools included), keeping the chain hash intact and entries compact | **VERIFIED** — `runTool`'s defer records one chained entry per tool call (Action `tool_call`, Resource=tool, Result `allowed`/`error`; pre-dispatch rejections record `blocked`), AgentID from `agent_id` arg else `default`; `Server.toolAudit()` lazily wires a persisted `governance.AuditLog` into the project `.kern/audit` (same store/lock/chain as platform+CLI), `KERN_MCP_AUDIT_DIR` override; mcp `TestMain` isolates the store from the package dir. Tests: `TestToolCallsAudited`, `TestAuditToolCallBlockedResult`, `TestAuditToolCallNilArgsSafe`. Live e2e: real stdio `kern_search` appended chained entry `audit-1085`. Follow-up CLOSED (2026-09-02): `storage.LogStore` — append-only `chain.jsonl` (O(1) per Put), legacy per-key merge-read, `storage.TailReader.LastEntry()` fast path in `AuditLog.refreshTailLocked` (prevHash + `audit-N` continuation); all 7 audit sites swapped from `NewLocal`; verified live on the repo's 1096-entry legacy dir (one tool call → `chain.jsonl` entry `audit-1097`, zero new per-key files, `kern audit` renders merged history). Known limit: `RepairChain` on a pure-jsonl store appends rather than heals in place |
| G-3 | Sandbox manifest network audit | Manifest is FS write-side only: Change{Path,Kind,Size,Hash} computed before rollback | Record network policy (netns state / KERN_ALLOW_NET / KERN_ALLOW_NO_ISOLATE) in the manifest + surface network-related errors from output; document read-audit limitation (no syscall tracing in a zero-dep build) | **VERIFIED** — new `sandbox.NetworkPolicy` on `Result.Network` (set in Run right after CombinedOutput): `Isolated` (honestly false for sandbox.Run — snapshot restores files but does NOT block egress; script runtime isolates fail-closed), `NetnsAvail` (GOOS==linux, matching doctor), `AllowNetEnv` (KERN_ALLOW_NET/KERN_ALLOW_UNISOLATED), `Hits` (network-error signatures matched in output, deduped, capped 5 — dial tcp/connection refused/no such host/…); `Summary()` renders one line; surfaced in `kern sandbox` text + `--json` "network" + kern_sandbox MCP output. Zero-dep ⇒ no per-syscall tracing (documented on the type). Tests: `TestAssessNetwork*` (3) + `TestRunRecordsNetworkPolicy`. Live: `kern sandbox -- echo "dial tcp…connection refused"` reports the posture + both signatures |
| G-4 | Taint scaffold scope | sec.TaintLite + GenTestScaffold are Go-only, whole-file scans | Add diff/range-scoped generation (only functions touched by a git range) and pytest scaffolds for Python sinks | **VERIFIED** — `sec.ScanPythonFile` (7 rules: py-eval/exec/os-system/subprocess(-shell)/pickle-load/yaml-load/sql-format; comment lines skipped; shell=True + Loader= conditions honored) wired into `sec.Scan`; `sec.FilterByFiles` + `--range from..to` CLI/MCP `range` arg (intersects with `--file`, prints "scope: N file(s) changed", invalid range → exit 2); `sec.GenPytestScaffold` + `sec.ScaffoldFor` dispatcher (`<name>_taint_test.py`, `test_<rule>_<line>` with dashes→underscores, TODO probe); `TaintLite` short-circuits python findings via source-expression heuristic (nil-index safe); pack.go's direct `ScanFile` call also gained the .py pass (orchestrator). 15 new tests incl. MCP round-trips; full suite 87/87 PASS. Live: pytest scaffold + range scoping verified on a temp project. Delegated to @fixer, reviewed + verified by orchestrator |
| G-5 | Index parallelism (directory sharding) | File-level worker pool (GOMAXPROCS, ≥256 jobs, KERN_INDEX_SERIAL) + incremental rebuilds (2443259) exist; proposal asks for directory-level sharding | Benchmark serial vs parallel build on this repo; close with evidence if file-level pool saturates, else implement sharding | **CLOSED (already sufficient)** — measured `index.Build(".")` on this repo (7,902 symbols, ~490 files): parallel 540/447ms vs serial 678/734ms ≈ **1.43x**. File-level pooling already saturates GOMAXPROCS; directory sharding adds complexity for marginal gain on top of this + incremental rebuilds. No change; evidence recorded 2026-09-02 |
| G-6 | Stale docs/claims | README/docs say 84 MCP tools (actual 86), "17+" agent profiles (actual 12 detectors/15 names), boundaries.json example schema wrong (actual: rules[{from,to,action}]), kern-server described "read-only L0" but has 17+ mutating POST routes | Fix in-repo docs; for the server claim fix the doc comment (or gate routes behind a flag — decide during work) | **VERIFIED** — fixed: README 84→86 (×2) + "17+ agent surfaces"→"12 built-in agent surfaces plus custom adapters" + "and 12 more agents"→"and 8 more"; internal/mcp/server.go (×2) + usage_guide.go 84→86; dropped the false "read-only" adjectives from internal/web/web.go package doc + App comment and cmd/kern-server/main.go (the console serves read views AND governance write endpoints — already disclosed further down the same doc comment). Not-in-repo (no fix needed): the wrong boundaries.json schema example (source/forbiddenImports) exists only in the EXTERNAL planning doc; README's boundaries section is prose-only (no schema example). Authoritative counts confirmed: 86 tools (Tool table; dispatch has 94 cases incl. aliases), 12 setup detectors |

## Wave 2 — MISSING verdicts (after Wave 1)

| ID | Gap | Fix | Status |
|----|-----|-----|--------|
| G-7 | SQLite/cache TTL + dormant archival | TTL-based eviction of stale per-project caches + gzip (compress/gzip, stdlib) archival of dormant project indexes — the "active in RAM, dormant on disk" half of report STEP 3 | **VERIFIED** — `cache.Maintain` (non-recursive walk: evicts files older than TTL **and their .gz twins**, gzips dormant plain .json >4 KiB to `<name>.json.gz` with mtime preserved, skips tiny/non-json/subdirs; dry-run mode); `MaintainDefaults` via `KERN_CACHE_ARCHIVE_DAYS` (7) / `KERN_CACHE_TTL_DAYS` (30), <=0 disables, garbage→default; `MaintainOnce` rate-limited by `.maintained-at` marker (1h, best-effort) wired into Store/Load on the data/ subdir (the dir those calls actually serve — cache root walk would be a no-op); Load/Exists/Store transparently handle .gz twins (Store re-writes plain + drops the twin). New `kern cache` CLI (dir, entries, size, maintain, --dry-run). 13 new tests; full suite 88/88 PASS. Live: `kern cache --dry-run` then `kern cache` archived 1 + evicted 1 on a scratch cache with mtime-preserved .gz. Delegated to @fixer, reviewed + verified by orchestrator |
| G-8 | LSP integration (`kern lsp`) | Minimal stdio JSON-RPC LSP server: initialize/shutdown, textDocument/hover, textDocument/definition, textDocument/references — powered by the prebuilt index (build once at start). Zero-dep | **VERIFIED** — `internal/lsp` (674 lines): Content-Length framed JSON-RPC 2.0; initialize (rootUri → workspaceFolders → --root, index load-or-built once per session), initialized, shutdown/exit, hover (markdown: name/kind/file:line/reconstructed signature), definition (Location, 0-based LSP lines), references (definition + caller declarations via ix.CallersFor, deduped), $/* ignored, -32700 on parse errors keeps serving, -32601 unknown requests; exact UTF-16→byte conversion; stdout carries only framed responses (diagnostics on stderr). `kern lsp [--root]` with SIGINT/SIGTERM grace. Known v1 limits documented (line-level references; signature-not-doc hover). 9 tests incl. end-to-end fixture session; orchestrator live-verified a framed session (hover on Twice → markdown, exit 0). Commit a3131a0. Delegated to @fixer, reviewed + verified by orchestrator |
| G-9 | MinHash log clustering | Sub-quadratic near-duplicate clustering (MinHash/simhash + banding via hash/fnv) for large logs, alongside the existing Levenshtein O(n²) pass | **VERIFIED** — `internal/compress/minhash.go`: char 3-gram shingles → 32 seeded FNV-1a MinHash signature values, each acting as its own LSH band (r=1: false candidates cost one cheap ratio check while a missed pair would split a family, so minimizing (1−J)^32 is the right trade); `bandIndex` maps (slot,value)→positions; candidates deduped, ascending, capped at 128 in first-occurrence order. clusterLines switches its fuzzy-merge pass to the banded path above 512 cluster lines; eligibility criteria unchanged — bands only decide WHICH pairs are compared, so a false merge is impossible. Two fixer sessions glitched (empty reports); implemented directly by the orchestrator after tests caught a whole-line-hash design bug (FNV is not locality-sensitive) and it was rewritten to shingle MinHash. 5 new tests: large-log families (50×100 exact + 3×30 fuzzy + noise), determinism, threshold boundary (pairwise ≡ banded, byte-identical), degenerate 2000+500 families, signature properties. Full compress suite PASS |
| G-11 | Blueprint duplication debt | 9 confirmed-block file pairs in the pre-commit hook's registry — all involving `internal/mcp/server.go` (vs task.go ×3, helpers.go ×2, docsearch.go, project.go, verify.go) plus `platform.go ↔ context/rules.go` (the `fmtRuntimeEventStr` mirror). Pre-existing; surfaced during the 2026-09-02 round | Cheap tier: consolidate small mirrored helpers into their canonical packages (runtime-event renderer first). Expensive tier: extract server.go's shared plumbing (write/Inflight/loadOrBuild patterns) and decompose the dispatch monolith — one pair per commit, hook block-count is the progress metric | **DONE (2026-09-02)** — ALL confirmed-blocks cleared (the original 9 + 2 newly-surfaced: rules↔guard, sec↔docsearch); the blueprint gate now passes clean on every touched file set. Extractions: `runtime.FormatEvent` (03c6055, cleared platform↔rules); `index.LoadOrBuild` (735003e, cleared server↔helpers ×2; task ×3 cleared with the fingerprint refresh); itoa family extinct in mcp/sec/docsearch/intel/verify → `strconv.Itoa`, `intel.DirMatch` canonical (rules.go mirror deleted), `optimize.EnsureRecorder` canonical (mcp/cmd/kern/kern-mcp delegates) (e3931a0 — cleared server↔verify/docsearch/project + rules↔guard + sec↔docsearch). `within`(mcp) vs `withinAbs`(verify) deliberately kept — genuine semantic difference (Windows drive-root containment), cross-documented. HOOK PROTOCOL (memory #305) — SUPERSEDED 2026-09-03: the fingerprint cache used to go stale on the first run after a dedup, forcing a no-op `git commit --amend --no-edit` ritual. The pre-commit hook now execs `scripts/blueprint-precommit.sh` (self-healing adapter): it prunes stale fingerprint entries (sha256-vs-content_hash mismatch or vanished files) BEFORE `blueprint check --staged` runs, so the first run after any edit/dedup starts from a fresh cache for exactly the changed files. Verified: 5 stale entries (edited mcp/setup/storage files) pruned → blueprint PASS exit 0; second run idempotent (0 stale, no prune). Install: `blueprint install hook` then replace the hook body with the tracked adapter (or exec it from the hook). Advisory polish COMPLETE (2026-09-02): itoa family EXTINCT repo-wide — the last 7 packages (pii/memory/commitmsg/learning/code/whatif/index, 21 call sites, every caller verified non-negative) now use `strconv.Itoa`, net −136 lines. pct↔bench.pctf and truncateMCP↔yaml.nextIndent left deliberately: semantically different template matches, not true duplication |

Removed from tracking (maintainer decision, 2026-09-02): the SQLite WAL/FTS5 "persistent index virtualization" (G-7's JSON-cache GC covers the practical need; reopen only with measured large-repo latency data) and the SentencePiece tokenizer (formerly G-10, declined — model-aware routing via KERN_TOKENIZER/KERN_MODEL covers the space).

## Verification protocol (per item)

1. `go build ./... && go vet ./...`
2. Targeted package tests for touched packages (`go test -short ./<pkg>/...`)
3. CLI dry-run / live execution of the touched command
4. Full `go test -short ./...` at wave boundaries
5. Commit with kern_commitmsg-derived message referencing the G-ID
