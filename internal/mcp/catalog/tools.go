// Tool registration table — the single source of truth for the kern MCP
// catalog. Kept in its own file so the registration table
// does not bury the server core (dispatch, transports, filtering) in
// server.go; the "expensive tier" plan keeps per-domain extraction as a
// follow-up. The catalog parity invariants (plugin <-> MCP, docs <-> MCP)
// read this table via ToolNames().

package catalog

// All is the full kern MCP tool catalog — the single source of truth for
// tool registration, consumed by the server, plugins, and docs via ToolNames().
var All = []Tool{
	{
		Name: "kern_optimize_prompt", Category: CategoryOptimize,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Compress a raw prompt before sending to an LLM; returns optimized text plus token savings. Non-local OLLAMA_HOST auto-masks secrets/PII.",
		InputSchema: schema(map[string]any{
			"prompt":       strProp("The raw prompt text to optimize"),
			"attached_log": strProp("Optional noisy log output to compress and attach"),
			"session":      strProp("Optional session identifier for stats tracking"),
			"model":        strProp("Optional model name for cost estimation"),
			"mask":         strProp("If true, strip secrets/PII before processing and restore placeholders (default false; auto-enabled for non-local LLM hosts)"),
			"mask_names":   strProp("Comma-separated client/project names to mask as [MASKED_NAME_N]"),
			"cache":        strProp("If true, serve identical requests from the local response cache (default false)"),
			"few_shot":     strProp("If true, inject top recalled lessons from project memory as baseline examples (default false)"),
			"root":         strProp("Project root used for few-shot memory (defaults to current directory)"),
		}, []string{"prompt"}),
	},
	{
		Name: "kern_fetch_raw_anchor", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Fetch raw text segments truncated by kern (anchor markers); pulls full logs/code slices on demand.",
		InputSchema: schema(map[string]any{
			"anchor_id": strProp("The anchor identifier emitted in the compressed output (e.g. anchor-a1b2c3d4e5f6)"),
		}, []string{"anchor_id"}),
	},
	{
		Name: "kern_llm_providers", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "List the LLM provider chain in priority order (Ollama first, then wired agent CLIs).",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"probe": strProp("If true, live-test each installed LLM provider (may take minutes)"),
		}, nil),
	},
	{
		Name: "kern_register_host_sampler", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Register/unregister a host sampler command for LLM delegation (sh -c; prompt on stdin, $KERN_SYSTEM_PROMPT = system prompt); empty unregisters. Registration is exec-gated: KERN_ALLOW_EXEC=1 or KERN_TOOLS must name kern_register_host_sampler; runs with a minimal PATH/HOME/TMPDIR env.",
		InputSchema: schema(map[string]any{
			"command": strProp("Shell command per generation (stdin = prompt, $KERN_SYSTEM_PROMPT = system prompt); empty = unregister"),
			"key":     strProp("Optional registration key (default: this connection's slot); per-agent/per-repo namespaces coexist"),
			"timeout": strProp("Per-call timeout in seconds (default 180; env KERN_HOST_SAMPLER_TIMEOUT)"),
			"model":   strProp("Optional model label reported with the registration"),
		}, []string{"command"}),
	},
	{
		Name: "kern_validate_staged", Category: CategoryGovernance,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "high",
		Description:   "Blueprint change firewall: validate the STAGED diff (git diff --cached) against policy.",
		InputSchema: schema(map[string]any{
			"root":   strProp("Repository root (default: server cwd)"),
			"source": strProp("Agent identity for provenance stamping (default: agent)"),
		}, nil),
	},
	{
		Name: "kern_validate_proposed", Category: CategoryGovernance,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "high",
		Description:   "Blueprint change firewall: validate a PROPOSED change (files [{path,content,op}]) against policy.",
		InputSchema: schema(map[string]any{
			"root":   strProp("Repository root (default: server cwd)"),
			"source": strProp("Agent identity for provenance stamping (default: agent)"),
			"files":  strProp("JSON array of proposed file changes: [{\"path\":\"a.go\",\"content\":\"...\",\"op\":\"add\"}]"),
		}, []string{"files"}),
	},
	{
		Name: "kern_explain_finding", Category: CategoryGovernance,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Description:   "Blueprint change firewall: explain a gate finding (rule id, severity, category, file, line, message, evidence).",
		InputSchema: schema(map[string]any{
			"root":    strProp("Repository root (default: server cwd)"),
			"finding": strProp("JSON object of the finding: {\"rule_id\":\"...\",\"severity\":\"error\",\"category\":\"secret\",\"file\":\"...\",\"line\":1,\"message\":\"...\"}"),
		}, []string{"finding"}),
	},
	{
		Name: "kern_repair_guidance", Category: CategoryGovernance,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Description:   "Blueprint change firewall: repair guidance for a gate finding — suggested fix, suppression, rule reference.",
		InputSchema: schema(map[string]any{
			"root":    strProp("Repository root (default: server cwd)"),
			"finding": strProp("JSON object of the finding (same shape as kern_explain_finding)"),
		}, []string{"finding"}),
	},
	{
		Name: "kern_optimize_output", Category: CategoryOptimize,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Compress an LLM response (assistant output): strip filler/hedge language, preserve code, lists, errors.",
		InputSchema: schema(map[string]any{
			"text": strProp("The LLM output text to compress"),
		}, []string{"text"}),
	},
	{
		Name: "kern_memory_add", Category: CategoryMemory,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Persist a distilled cross-session lesson for a project (the project 'brain').",
		InputSchema: schema(map[string]any{
			"lesson": strProp("The lesson to remember, e.g. 'deploy tags are pushed from a manual release workflow, not CI'"),
			"root":   strProp("Project root whose memory store to append (defaults to current directory)"),
		}, []string{"lesson"}),
	},
	{
		Name: "kern_memory_list", Category: CategoryMemory,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "List stored lessons for a project, most recent first with timestamps.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_memory_recall", Category: CategoryMemory,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Recall up-to-k most relevant past lessons for a prompt by keyword overlap; deterministic, local.",
		InputSchema: schema(map[string]any{
			"prompt": strProp("Query to match lessons against"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit":  strProp("Max lessons to return (default 5)"),
		}, []string{"prompt"}),
	},
	{
		Name: "kern_mask_pii", Category: CategoryIncident,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Locally scan text for secrets/PII (API keys, passwords, tokens, URLs, emails).",
		InputSchema: schema(map[string]any{
			"text":       strProp("The raw text to mask"),
			"mask_names": strProp("Optional comma-separated client/project names to mask"),
		}, []string{"text"}),
	},
	{
		Name: "kern_security", Category: CategoryIncident,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "high",
		Description:   "Local security scan of project source.",
		InputSchema: schema(map[string]any{
			"root":     strProp("Project root to scan (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"severity": strProp("Comma-separated severities to include: error,warning,info (default all)"),
			"max":      strProp("Max findings to return (default 100; 0 = no cap)"),
			"format":   strProp("Output format: text or json (default text)"),
		}, nil),
	},
	{
		Name: "kern_safe_delete", Category: CategoryIncident,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "high",
		Description:   "Check whether a symbol can be safely deleted.",
		InputSchema: schema(map[string]any{
			"symbol": strProp("Symbol name (simple name like 'greet' or qualified like 'User.Login')"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"format": strProp("Output format: text or json (default text)"),
		}, []string{"symbol"}),
	},
	{
		Name: "kern_doc_search", Category: CategoryDoc,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Local vector search over project docs (markdown, text, rst, adoc) with deterministic n-gram hashing.",
		InputSchema: schema(map[string]any{
			"query": strProp("Natural-language or keyword query"),
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"k":     strProp("Max fragments to return (default 5)"),
		}, []string{"query"}),
	},
	{
		Name: "kern_doc_index", Category: CategoryDoc,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Pre-index project docs for kern_doc_search; semantic=true adds local Ollama embeddings (KERN_EMBED_MODEL).",
		InputSchema: schema(map[string]any{
			"root":     strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"semantic": strProp("If true, add dense Ollama embeddings (requires a local Ollama with the model pulled)"),
		}, nil),
	},
	{
		Name: "kern_doc_fetch", Category: CategoryDoc,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Fetch a public doc page into the project doc index — the ONLY network call in kern, explicit. semantic=true adds embeddings.",
		InputSchema: schema(map[string]any{
			"url":      strProp("https URL of the documentation page to fetch"),
			"root":     strProp("Project root whose doc index receives the page (defaults to current directory)"),
			"name":     strProp("Optional index name (default derived from the URL host+path)"),
			"semantic": strProp("If true, attach dense embeddings via the local Ollama model (skipped when the model is unavailable)"),
		}, []string{"url"}),
	},
	{
		Name: "kern_commitmsg", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "low",
		Description:   "Generate a deterministic conventional-commit message (type, scope, subject, per-file body) from the git diff.",
		InputSchema: schema(map[string]any{
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"staged": strProp("If true, read the staged diff (git diff --cached) instead of the working tree vs HEAD"),
			"range":  strProp("Optional commit range like a..b; overrides staged and HEAD defaults"),
		}, nil),
	},
	{
		Name: "kern_precache", Category: CategoryOptimize,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "medium",
		Description:   "Scan the project once and fill code-summary and document-vector caches so later kern calls are instant.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_swap", Category: CategoryEdit,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "low",
		Description:   "Swap fenced code blocks tagged lang:path to per-file symbolic signatures to fit a token budget, or expand them back",
		InputSchema: schema(map[string]any{
			"text":       strProp("The context document containing fenced code blocks"),
			"root":       strProp("Project root used to resolve block paths (defaults to current directory)"),
			"max_tokens": strProp("Token budget; if the document exceeds it, blocks are swapped to summaries"),
			"mode":       strProp("force mode: summary, expand, or fit (default)"),
		}, []string{"text"}),
	},
	{
		Name: "kern_sandbox", Category: CategoryExec,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "critical",
		Description:   "Run a risky command inside a project snapshot: non-zero exit rolls back exactly; HIGH-risk touches restore unless force=true. Rollback-on-failure, NOT isolation: full user privileges, out-of-root writes never rolled back, file-borne secrets readable. Gated by KERN_ALLOW_EXEC / KERN_TOOLS.",
		InputSchema: schema(map[string]any{
			"root":    strProp("Project root to snapshot and run in (defaults to current directory)"),
			"command": strProp("Full command to run, e.g. \"make migrate\" or \"sh -c 'npm test'\" (shell words, not a shell string)"),
			"timeout": strProp("Timeout in seconds (default 120)"),
			"force":   strProp("If true, keep changes even when touched files carry a HIGH pre-edit verdict (default restores)"),
		}, []string{"command"}),
	},
	{
		Name: "kern_diff_files", Category: CategoryEdit,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Description:   "Compute a unified line diff between two files (or versions) using pure Go.",
		InputSchema: schema(map[string]any{
			"a":    strProp("Path to the old/base file"),
			"b":    strProp("Path to the new/changed file"),
			"root": strProp("Project root; when set, a and b must stay inside it (stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"a", "b"}),
	},
	{
		Name: "kern_heal", Category: CategoryExec,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "high",
		Description:   "Self-correction loop: run validation, ask a local Ollama model to fix failing files in a throwaway snapshot, re-validate. Never edits the working tree.",
		InputSchema: schema(map[string]any{
			"root":       strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"task":       strProp("The original task the code is meant to fulfil"),
			"model":      strProp("Ollama model (default KERN_MODEL or llama3.2)"),
			"max_rounds": strProp("Correction attempts (default 3)"),
			"timeout":    strProp("Validation timeout in seconds (default 120)"),
			"force":      strProp("If true, attempt repairs even when the failing files carry a HIGH pre-edit verdict (default refuses)"),
		}, nil),
	},
	{
		Name: "kern_validate", Category: CategoryExec,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "high",
		Description:   "Auto-validation: detect and run the project's build/test command.",
		InputSchema: schema(map[string]any{
			"root":    strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"command": strProp("Optional override command, e.g. \"go test ./...\" (defaults to auto-detected)"),
			"timeout": strProp("Timeout in seconds (default 120)"),
			"raw":     strProp("Emit the raw command output without the report envelope (run_build-style compact result)"),
		}, nil),
	},
	{
		Name: "kern_schema_validate", Category: CategoryVerify,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Validate JSON output against a JSON schema (subset: primitives, required, enum, bounds, pattern, additionalProperties); one line per violation.",
		InputSchema: schema(map[string]any{
			"data":   strProp("The JSON output to validate"),
			"schema": strProp("The JSON schema to validate against"),
		}, []string{"data", "schema"}),
	},
	{
		Name: "kern_verify_output", Category: CategoryVerify,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Hallucination check: extract file:line, symbol and route references from agent output and confirm against the source tree; ok/MISS verdicts.",
		InputSchema: schema(map[string]any{
			"text": strProp("The agent output text to verify"),
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"text"}),
	},
	{
		Name: "kern_check_draft", Category: CategoryVerify,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Validate agent draft code against the project index (lighter than LSP).",
		InputSchema: schema(map[string]any{
			"code": strProp("The draft code to validate"),
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"lang": strProp("Code language: \"go\" or empty for Go; any other language is skipped conservatively"),
		}, []string{"code"}),
	},
	{
		Name: "kern_taint", Category: CategoryIncident,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "high",
		Description:   "Taint-lite analysis: flag security sinks (SQL/command injection, unsafe deserialization, Python eval/exec/yaml) reachable from entry points; optional git range.",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root to scan (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"file":  strProp("Optional filter: only findings in this file path"),
			"range": strProp("Optional git range 'from..to' to scope findings to changed files; '..' means the working tree"),
		}, nil),
	},
	{
		Name: "kern_compact_file", Category: CategoryContext,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Return a compact symbolic summary of a source file (functions, types, line numbers).",
		InputSchema: schema(map[string]any{
			"path": strProp("Absolute or relative path of the file to summarize"),
			"root": strProp("Project root; when set, path must stay inside it (stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"tier": strProp("'summary' (default), 'full', or 'folded'"),
		}, []string{"path"}),
	},
	{
		Name: "kern_fit_context", Category: CategoryContext,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "low",
		Description:   "Fit targeted files, symbols or queries into a token budget via tiered AST folding (Full Source -> Signatures ->.",
		InputSchema: schema(map[string]any{
			"root":       strProp("Project root directory"),
			"max_tokens": strProp("Token budget (default 8000, e.g. 4000, 8000, 32000, 128000)"),
			"files":      strProp("Comma-separated list of target files"),
			"symbols":    strProp("Comma-separated list of target symbol names"),
			"query":      strProp("Optional query to find and include relevant symbols"),
			"format":     strProp("'text' (default) or 'json'"),
		}, nil),
	},
	{
		Name: "kern_project_map", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Description:   "Return a compressed map of a whole project: every source file with its symbols and line counts.",
		InputSchema: schema(map[string]any{
			"root":      strProp("Project root directory"),
			"max_files": strProp("Maximum number of files to include (default 500)"),
		}, []string{"root"}),
	},
	{
		Name: "kern_pack", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "low",
		Description:   "Pack a whole project into one paste-ready bundle (instructions, tree, contents) sized to max_tokens.",
		InputSchema: schema(map[string]any{
			"root":         strProp("Project root directory"),
			"max_tokens":   strProp("Token budget for the bundle (default 8000; 0 = unlimited)"),
			"format":       strProp("'text' (default) or 'json'"),
			"instructions": strProp("'true' to include root-level docs as instructions (default), 'false' to skip them"),
			"fold":         strProp("'true' to pack tier=folded content (signatures kept, bodies elided with line counts)"),
			"tier":         strProp("Content tier: 'full' (default), 'folded', or 'summary'"),
			"graph":        strProp("'true' to pack the graph-snapshot mode (adjacency + signatures + per-file SHA-256) instead of contents"),
			"symbol":       strProp("Symbol to pack as a subgraph (graph mode only; empty = whole graph; ignored when graph=false)"),
		}, []string{"root"}),
	},
	{
		Name: "kern_buddy", Category: CategoryMeta,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Description:   "Session onboarding digest: the project's conventions, layout, entry points and gotchas distilled from the index, docs, history.",
		InputSchema: schema(map[string]any{
			"root":       strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"max_output": strProp("Raise the MCP output sandbox cap for this call (bytes; 0 disables)"),
		}, nil),
	},
	{
		Name: "kern_optimize_log", Category: CategoryOptimize,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Strip noise from log output: keep errors, warnings, stack traces, build failures.",
		InputSchema: schema(map[string]any{
			"log": strProp("The log text to compress"),
		}, []string{"log"}),
	},
	{
		Name: "kern_context_budget", Category: CategoryContext,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "low",
		Description:   "Fit text into a token budget: deduplicate lines, keep head plus important lines (errors, stack frames), then trim.",
		InputSchema: schema(map[string]any{
			"text":       strProp("The text (log output, file dump, conversation) to fit into the budget"),
			"max_tokens": strProp("Maximum tokens the result may use (default 4000)"),
		}, []string{"text"}),
	},
	{
		Name: "kern_stats", Category: CategoryOptimize,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Return before/after token savings and cost estimates from kern optimizations, optionally filtered.",
		InputSchema: schema(map[string]any{
			"days":    strProp("Aggregate over the last N days (default 7)"),
			"session": strProp("Filter to a session identifier"),
		}, nil),
	},
	{
		Name: "kern_semcache", Category: CategoryOptimize,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Inspect/manage the semantic cache serving similar prior queries: stats (default), list, clear, or similarity.",
		InputSchema: schema(map[string]any{
			"action":    strProp("'stats' (default), 'list', 'clear', or 'similarity'"),
			"namespace": strProp("prompt or log (default: all)"),
			"a":         strProp("First input for similarity"),
			"b":         strProp("Second input for similarity"),
		}, []string{"action"}),
	},
	{
		Name: "kern_ast_search", Category: CategoryAST,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "AST-level symbol search across a Go project: patterns like 'func greet', 'type *User*'.",
		InputSchema: schema(map[string]any{
			"pattern": strProp("Symbol pattern. Prefixes: func, method, struct, interface, type, const, var. '*' wildcards supported"),
			"root":    strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit":   strProp("Max results (default 50)"),
		}, []string{"pattern"}),
	},
	{
		Name: "kern_frameworks", Category: CategoryFramework,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Detect the frameworks/libraries a project uses (Spring, Rails, Django, Express, gin, ...) by scanning manifests.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_fw_trace", Category: CategoryFramework,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Framework DI & route tracing: maps pipelines across Gin, Express, FastAPI, Spring Boot, NestJS.",
		InputSchema: schema(map[string]any{
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"filter": strProp("Optional keyword or path filter to match specific routes/handlers"),
			"format": strProp("'text' (default) or 'json'"),
		}, nil),
	},
	{
		Name: "kern_entry_points", Category: CategoryFramework,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "List framework entry points from the index: handlers, controllers, route targets with framework and route.",
		InputSchema: schema(map[string]any{
			"root":    strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit":   strProp("Max results (default 50)"),
			"pattern": strProp("Optional route/name wildcard filter, e.g. '*admin*'"),
		}, nil),
	},
	{
		Name: "kern_search", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Ranked free-text symbol search: symbols matching a query by name or file, best first; camelCase/plural/accent forgiving. semantic=true re-ranks via Ollama.",
		InputSchema: schema(map[string]any{
			"query":    strProp("Free-text query (symbol name, path fragment, or partial name)"),
			"root":     strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit":    strProp("Max results (default 20)"),
			"semantic": strProp("When 'true', re-rank by Ollama dense embeddings (requires embedding model)"),
		}, []string{"query"}),
	},
	{
		Name: "kern_prose", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Prose-word to symbol candidate lookup.",
		InputSchema: schema(map[string]any{
			"query": strProp("Prose words to resolve to symbol candidates (e.g. 'build security')"),
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit": strProp("Max results (default 20)"),
		}, []string{"query"}),
	},
	{
		Name: "kern_repo_search", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Description:   "Ranked free-text symbol search across every repo in the kern multi-repo registry.",
		InputSchema: schema(map[string]any{
			"query":    strProp("Free-text query (symbol name, path fragment, or partial name)"),
			"limit":    strProp("Max results (default 20)"),
			"semantic": strProp("When 'true', re-rank by Ollama dense embeddings (requires embedding model)"),
		}, []string{"query"}),
	},
	{
		Name: "kern_why", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Rationale and doc-reference report for a symbol: doc comment, who depends on it and why, in/out edge counts.",
		InputSchema: schema(map[string]any{
			"symbol":         strProp("Symbol name or Receiver.Name"),
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"min_confidence": strProp("Prune caller rows below this provenance tier: EXTRACTED, INFERRED or AMBIGUOUS (default: keep all)"),
		}, []string{"symbol"}),
	},
	{
		Name: "kern_inherits", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Return the inheritance edges of a symbol: supertypes (extends/implements/embeds) and subtypes.",
		InputSchema: schema(map[string]any{
			"symbol": strProp("Symbol name (e.g. 'Item' or 'Logger')"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"symbol"}),
	},
	{
		Name: "kern_context", Category: CategoryContext,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Return the minimal relevant source slice for a symbol: definition source, callers, and what it calls.",
		InputSchema: schema(map[string]any{
			"symbol":         strProp("Symbol name (e.g. 'greet')"),
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"lines":          strProp("Lines of source context around the definition (default 12)"),
			"agent_id":       strProp("Agent identity for governed mode (P1.2): scopes results to what this agent may read; omit for raw mode."),
			"task":           strProp("Task ID for governed mode; pairs with agent_id to scope authorization to the task paths."),
			"scope":          map[string]any{"type": "object", "description": "Optional task scope object {paths, denied_paths, services, envs, artifacts} for governed mode."},
			"max_tokens":     strProp("Optional token budget cap; when provided, fits the slice within it deterministically"),
			"with_freshness": strProp("When true, append the index freshness-proof footer"),
			"lens":           strProp("Review lens: security, performance, maintainability, architecture, balanced, or a combined preset."),
			"profile":        strProp("Output profile: machine-json, action-first, human-readable, or debug (default human-readable)."),
		}, []string{"symbol"}),
	},
	{
		Name: "kern_changes", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Description:   "Line-aware change-impact analysis for a diff.",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"range": strProp("Git range like 'HEAD~2..HEAD'. Empty = working-tree changes"),
			"file":  strProp("Optional comma-separated explicit file list, overrides git range"),
		}, nil),
	},
	{
		Name: "kern_review", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Description:   "Token-optimised code-review context for changed files.",
		InputSchema: schema(map[string]any{
			"root":       strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"range":      strProp("Git range like 'HEAD~2..HEAD'. Empty = working-tree changes"),
			"file":       strProp("Optional comma-separated explicit file list, overrides git range"),
			"max_tokens": strProp("Maximum tokens for the review context (default 8000)"),
			"lens":       strProp("Review lens: security, performance, maintainability, architecture, balanced, or a combined preset."),
			"profile":    strProp("Output profile: machine-json, action-first, human-readable, or debug (default human-readable)."),
		}, nil),
	},
	{
		Name: "kern_hubs", Category: CategoryArch,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Architectural hotspots: the most depended-on symbols (hubs) and cross-package bridges.",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit": strProp("Max hubs to return (default 10)"),
		}, nil),
	},
	{
		Name: "kern_test_gaps", Category: CategoryVerify,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Test-coverage analysis from the call graph: percent of callable symbols exercised by tests, plus untested hotspots.",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit": strProp("Max hotspots to list (default 10)"),
		}, nil),
	},
	{
		Name: "kern_mutation_test", Category: CategoryVerify,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "medium",
		Description:   "Lightweight mutation testing for test gaps: inverts conditions, flips booleans to find surviving mutants.",
		InputSchema: schema(map[string]any{
			"root":         strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"files":        strProp("Comma-separated list of target files to mutate (default: all project files)"),
			"max_mutants":  strProp("Maximum number of mutants to evaluate (default 20)"),
			"dry_run":      strProp("'true' to list potential mutants without executing test suite, 'false' (default) to run evaluation"),
			"test_command": strProp("Optional custom test command (default: go test ./<pkg> -count=1)"),
			"format":       strProp("'text' (default) or 'json'"),
		}, nil),
	},
	{
		Name: "kern_path", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Shortest call path between two symbols, following in-project call edges in either direction.",
		InputSchema: schema(map[string]any{
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"from":           strProp("Source symbol (simple name or Type.Method)"),
			"to":             strProp("Target symbol (simple name or Type.Method)"),
			"min_confidence": strProp("Search only edges at/above this provenance tier: EXTRACTED, INFERRED or AMBIGUOUS (default: every edge)"),
		}, []string{"from", "to"}),
	},
	{
		Name: "kern_dead", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Dead-code detection: symbols nothing in the project calls.",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit": strProp("Max entries (default all)"),
		}, nil),
	},
	{
		Name: "kern_cycles", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Package-level import cycles via Tarjan SCC over the project-local import graph.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"json": strProp("When 'true', return the structured cycles JSON instead of the text report"),
		}, nil),
	},
	{
		Name: "kern_larges", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Find the largest function/method declarations by source lines — god functions that beg for refactoring.",
		InputSchema: schema(map[string]any{
			"root":      strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"min_lines": strProp("Size threshold in source lines (default 60)"),
			"limit":     strProp("Max results (default all)"),
		}, nil),
	},
	{
		Name: "kern_arch", Category: CategoryArch,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Architecture overview from call-graph communities: subsystems with hubs/packages, plus coupling warnings.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_communities", Category: CategoryArch,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Call-graph communities (label propagation).",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit": strProp("Max communities to return (default all)"),
		}, nil),
	},
	{
		Name: "kern_churn", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Change-frequency risk.",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"range": strProp("Git range like 'HEAD~10..HEAD' (default last 30 commits)"),
		}, nil),
	},
	{
		Name: "kern_fragility_hotspots", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Correlate historical defect/fix commits with the AST symbol call graph to score fragility and flag regression-prone components.",
		InputSchema: schema(map[string]any{
			"root":      strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"target":    strProp("Optional specific file or symbol name to query"),
			"limit":     strProp("Max hotspots to return (default 15)"),
			"commits":   strProp("Commit history depth to evaluate (default 100)"),
			"min_fixes": strProp("Minimum defect fix commits required to qualify (default 1)"),
			"format":    strProp("'text' (default) or 'json'"),
		}, nil),
	},
	{
		Name: "kern_near", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Dependency-tree expansion: every symbol within N hops of a symbol (callers + callees), budget-capped.",
		InputSchema: schema(map[string]any{
			"symbol": strProp("Root symbol (simple name or Type.Method)"),
			"depth":  strProp("Number of hops to expand (default 2)"),
			"max":    strProp("Maximum nodes to return (default 100)"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"symbol"}),
	},
	{
		Name: "kern_probe", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Micro-context router.",
		InputSchema: schema(map[string]any{
			"task":           strProp("Natural-language task, bug report or error text mentioning symbols"),
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"max_tokens":     strProp("Token budget for the bundle (default 4000)"),
			"min_confidence": strProp("Prune anchor call rows below this provenance tier: EXTRACTED, INFERRED or AMBIGUOUS (default: keep all)"),
		}, []string{"task"}),
	},
	{
		Name: "kern_retrieve", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Retrieve context at progressive disclosure levels (L1=index summary, L2=neighborhood, L3=source) with stable handles; task_type selects the level.",
		InputSchema: schema(map[string]any{
			"query":          strProp("Free-text query for the L1 index summary (required at level l1)"),
			"symbol":         strProp("Symbol name for L2 neighborhood / L3 source retrieval (required at level l2/l3)"),
			"level":          strProp("Disclosure level: l1=index summary, l2=neighborhood, l3=source (default l2); mutually exclusive with task_type"),
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit":          map[string]any{"type": "integer", "description": "Max L1 items to return (default 10)"},
			"depth":          map[string]any{"type": "integer", "description": "L2 blast-radius depth in hops (default 0 = unlimited)"},
			"max_nodes":      map[string]any{"type": "integer", "description": "Max L2 neighborhood nodes to return (default 0 = unlimited)"},
			"lines":          map[string]any{"type": "integer", "description": "L3 source context lines around the definition (default 12)"},
			"max_tokens":     map[string]any{"type": "integer", "description": "Token budget for the rendered result (default 0 = no cap)"},
			"task_type":      strProp("Task type driving the disclosure level via planner policy (documentation=l1, refactor=l3, else l2); exclusive with level"),
			"with_freshness": map[string]any{"type": "boolean", "description": "When true, append a ---freshness-proof--- footer with the index's content-addressed freshness proof"},
		}, nil),
	},
	{
		Name: "kern_resolve", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Resolve a handle ID from kern_retrieve to L2 or L3 content, validating staleness via content hash.",
		InputSchema: schema(map[string]any{
			"handle":         strProp("Handle ID returned by kern_retrieve (e.g. 'a1b2c3...')"),
			"level":          strProp("Disclosure level to resolve to: l2=neighborhood, l3=source (default l2)"),
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"max_tokens":     map[string]any{"type": "integer", "description": "Token budget for the rendered result (default 0 = no cap)"},
			"with_freshness": map[string]any{"type": "boolean", "description": "When true, append a ---freshness-proof--- footer with the index's content-addressed freshness proof"},
		}, []string{"handle"}),
	},
	{
		Name: "kern_context_envelope", Category: CategoryContext,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Description:   "Return the assembled context envelope (domain.ContextPacket) as machine-readable JSON with schema versioning.",
		InputSchema: schema(map[string]any{
			"change":         strProp("Symbol name or change description to analyze into a context packet"),
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"max_tokens":     map[string]any{"type": "integer", "description": "Token budget for the fitted render (default 0 = no fitting)"},
			"with_freshness": map[string]any{"type": "boolean", "description": "When true, append a ---freshness-proof--- footer with the index's content-addressed freshness proof"},
		}, []string{"change"}),
	},
	{
		Name: "kern_plan_context", Category: CategoryAnalyze,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "low",
		Description:   "Deterministically plan which context to include for a change.",
		InputSchema: schema(map[string]any{
			"change": strProp("Symbol name or change description to plan context for"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"budget": map[string]any{"type": "integer", "description": "Token budget for the selection (default 0 = the policy's MaxTokens)"},
			"json":   map[string]any{"type": "boolean", "description": "When true, return the structured plan as JSON instead of rendered text"},
		}, []string{"change"}),
	},
	{
		Name: "kern_orchestrate", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Description:   "Run the silent context pipeline over an intent.",
		InputSchema: schema(map[string]any{
			"intent": strProp("Natural-language task or change description to orchestrate context for"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"budget": map[string]any{"type": "integer", "description": "Token budget for the fitted envelope (default 0 = the task policy's MaxTokens)"},
			"mode":   map[string]any{"type": "string", "description": "Context-mode preset overriding the policy family: fix | review | architecture | incident | explain (default empty = classify the intent)"},
			"skill":  map[string]any{"type": "string", "description": "Bundled agent skill whose runbook is appended to the delivered render: kern-investigate | kern-safe-change | kern-incident-triage (default empty = no skill section)"},
		}, []string{"intent"}),
	},
	{
		Name: "kern_skill", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Description:   "Catalog or load bundled agent skills (kern-investigate, kern-safe-change, kern-incident-triage).",
		InputSchema: schema(map[string]any{
			"action": strProp("catalog (default) to list skills, or load to return a skill's runbook"),
			"skill":  strProp("Skill name to load when action=load"),
		}, nil),
	},
	{
		Name: "kern_agent_message", Category: CategoryAgent,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "medium",
		Description:   "Send a message to an agent's coordination inbox (model as default sender); the target observes it via action=inbox.",
		InputSchema: schema(map[string]any{
			"to_agent":   strProp("Target agent id (required)"),
			"notes":      strProp("The message/directive to send (required)"),
			"from_agent": strProp("Sender id (default: the calling model)"),
			"task_id":    strProp("Optional task id to associate the message with"),
			"root":       strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"to_agent", "notes"}),
	},
	{
		Name: "kern_agent_interrupt", Category: CategoryAgent,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "high",
		Description:   "Cancel a running task by ID via TaskService: transitions to CANCELLED with a reason, persists, publishes task.",
		InputSchema: schema(map[string]any{
			"task_id": strProp("Task id to cancel (required)"),
			"reason":  strProp("Why the task is being interrupted (default: 'interrupted by model via kern_agent_interrupt')"),
			"root":    strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"task_id"}),
	},
	{
		Name: "kern_mcp_call", Category: CategoryMCPBridge,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "high",
		Description:   "Bridge a tool from an external MCP server configured via kern mcp add (.kern/mcp-servers.json).",
		InputSchema: schema(map[string]any{
			"server":    strProp("Configured server name (required; see kern mcp list)"),
			"tool":      strProp("Tool name to call — raw wire name or public mcp__<server>__<tool> (required)"),
			"arguments": map[string]any{"type": "object", "description": "JSON arguments object for the tool"},
			"root":      strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"server", "tool"}),
	},
	{
		Name: "kern_trace", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "low",
		Description:   "Runtime-impact overlay: parse a pprof dump, stack trace, or function list and map hot symbols onto the call graph — file:line, blast radius, coverage.",
		InputSchema: schema(map[string]any{
			"trace": strProp("The trace text (pprof -top, stack trace, or symbol list)"),
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit": strProp("Max hot symbols to return (default all)"),
		}, []string{"trace"}),
	},
	{
		Name: "kern_lock", Category: CategoryLock,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "medium",
		Description:   "Acquire an advisory flock-based workspace lock on a scope, held until kern_unlock; errors when already held.",
		InputSchema: schema(map[string]any{
			"scope": strProp("Lock scope, e.g. 'db-models' or 'checkout'"),
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"scope"}),
	},
	{
		Name: "kern_unlock", Category: CategoryLock,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "medium",
		Description:   "Release a workspace lock previously acquired via kern_lock.",
		InputSchema: schema(map[string]any{
			"scope": strProp("Lock scope to release"),
		}, []string{"scope"}),
	},
	{
		Name: "kern_lock_status", Category: CategoryLock,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "low",
		Description:   "List workspace locks with whether each is held and by which PID.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_guard_check", Category: CategoryGovernance,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "low",
		Description:   "Architectural guardrails: validate changed files against .kern/boundaries.json; return forbidden dependency crossings. format=sarif; threshold=N fails above N.",
		InputSchema: schema(map[string]any{
			"root":      strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"file":      strProp("Optional comma-separated explicit file list, overrides git range"),
			"range":     strProp("Git range like 'HEAD~2..HEAD'. Empty = working-tree changes"),
			"format":    strProp("Output format: text (default) or sarif"),
			"threshold": strProp("Fail (isError) when the violation count exceeds this number (default 0 = any violation fails)"),
		}, nil),
	},
	{
		Name: "kern_authorize_context", Category: CategoryGovernance,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Authorized-context primitive (P0.1): compute the symbols/call edges an agent may legally read, filtered by identity and task scope, with an auditable proof.",
		InputSchema: schema(map[string]any{
			"agent_id":      strProp("Agent ID to authorize (must be registered, e.g. via identity.RegisterAgent / kern_agent)"),
			"task":          strProp("Task ID the authorization is scoped to"),
			"root":          strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"symbol_filter": strProp("Optional substring filter applied to the allowed symbols only"),
			"scope":         strProp("Optional task scope object: {paths: [], denied_paths: [], services: [], envs: [], artifacts: []}"),
		}, []string{"agent_id", "task"}),
	},
	{
		Name: "kern_rename", Category: CategoryEdit,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "high",
		Description:   "Structural rename on the AST index: preview definitions/references for a Go package-level symbol, apply with apply=true (backups + rollback). Non-Go refused.",
		InputSchema: schema(map[string]any{
			"root":     strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"symbol":   strProp("Symbol to rename (package-level Go name, e.g. Widget)"),
			"new_name": strProp("New identifier"),
			"apply":    strProp("If true, commit the rename (with backups + rollback); otherwise return the preview only"),
			"force":    strProp("If true, apply even when the symbol carries a HIGH pre-edit verdict (default refuses)"),
		}, []string{"symbol", "new_name"}),
	},
	{
		Name: "kern_exec", Category: CategoryExec,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "critical",
		Description:   "Run code in an isolated runtime, return ONLY stdout. Fails closed without network isolation unless KERN_ALLOW_UNISOLATED=1 (alias KERN_ALLOW_NET=1).",
		InputSchema: schema(map[string]any{
			"code":       strProp("The script body (required)"),
			"lang":       strProp("Language override (e.g. python3, node, bash, go); otherwise detected from the shebang"),
			"timeout":    strProp("Timeout in seconds (default 10)"),
			"max":        strProp("Max stdout bytes to return (default 16384)"),
			"stdin":      strProp("Input piped to the script's stdin"),
			"list":       strProp("If true, return the installed runtimes and supported languages and do nothing else"),
			"no_isolate": strProp("Ignored unless the local operator sets KERN_ALLOW_NO_ISOLATE=1; isolation is enforced by default"),
			"egress":     map[string]any{"type": "array", "items": map[string]any{"type": "string"}, "description": "Egress targets (host:port) this script may contact; declarations are REQUIRED for network access on unisolated runs (deny-by-default). Comment declarations (# egress: / // egress:) are honored equivalently."},
		}, []string{"code"}),
	},
	{
		Name: "kern_explore", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "One-call symbol exploration: definition source, callers, callees, and blast radius up to N hops; optional why-rationale.",
		InputSchema: schema(map[string]any{
			"symbol":         strProp("Symbol name (e.g. 'greet' or 'User.Login')"),
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"depth":          strProp("Cap blast radius to N hops from the symbol (default 2, 0 = unlimited)"),
			"max":            strProp("Maximum blast-radius symbols to return (default 30)"),
			"explain":        strProp("When 'true', append the why-rationale section (what the symbol is, who depends on it and why)"),
			"agent_id":       strProp("Agent identity for governed mode (P1.2): scopes results to what this agent may read; omit for raw mode."),
			"task":           strProp("Task ID for governed mode; pairs with agent_id to scope authorization to the task paths."),
			"scope":          map[string]any{"type": "object", "description": "Optional task scope object {paths, denied_paths, services, envs, artifacts} for governed mode."},
			"with_freshness": strProp("When true, append the index freshness-proof footer"),

			"min_confidence": strProp("Prune callers/callees below this provenance tier: EXTRACTED, INFERRED or AMBIGUOUS (default: keep all)"),
			"max_tokens":     strProp("Token budget for source + callee skeletons (default: verbatim, no skeletons)")}, []string{"symbol"}),
	},
	{
		Name: "kern_graph", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "One-call graph context.",
		InputSchema: schema(map[string]any{
			"symbol":         strProp("Symbol name (simple name or Type.Method)"),
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"max_tokens":     strProp("Token budget for the names-only adjacency (default: adaptive, base 400 + 20/neighbor capped 2000; 0 = uncapped)"),
			"min_confidence": strProp("Prune adjacency rows below this provenance tier: EXTRACTED, INFERRED or AMBIGUOUS (default: keep all)"),
			"format":         strProp("'one-line' renders the single-line call-graph neighbourhood (kern_code_graph contract); default: adjacency report"),
			"entities":       strProp("When true, append the entity-node block: digital-twin entities (API endpoints, DB tables, topics, services, deployments) connected via twin edges"),
			"agent_id":       strProp("Agent identity for governed mode (P1.2): scopes results to what this agent may read; omit for raw mode."),
			"task":           strProp("Task ID for governed mode; pairs with agent_id to scope authorization to the task paths."),
			"scope":          map[string]any{"type": "object", "description": "Optional task scope object {paths, denied_paths, services, envs, artifacts} for governed mode."},
			"with_freshness": strProp("When true, append the index freshness-proof footer"),
		}, []string{"symbol"}),
	},
	{
		Name: "kern_fts_search", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "FTS5 full-text search over the SQLite symbol index (MATCH syntax like 'func AND greet').",
		InputSchema: schema(map[string]any{
			"query": strProp("FTS5 MATCH query over symbols"),
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit": strProp("Max results (default 20)"),
		}, []string{"query"}),
	},
	{
		Name: "kern_bridges", Category: CategoryArch,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Find cross-package bridges: symbols called from more than one package, ranked by how many packages they couple.",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit": strProp("Max bridges to return (default 15)"),
		}, nil),
	},
	{
		Name: "kern_lsp_bridge", Category: CategoryMCPBridge,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Description:   "Zero-weight LSP client bridge.",
		InputSchema: schema(map[string]any{
			"file":       strProp("Target source file path"),
			"line":       strProp("1-based line number (required for definition, hover, references)"),
			"column":     strProp("1-based column number (default 1)"),
			"action":     strProp("LSP query action: 'definition' (default), 'hover', 'references', 'symbols', or 'servers'"),
			"server_cmd": strProp("Optional custom language server command to override auto-detection"),
			"root":       strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"format":     strProp("'text' (default) or 'json'"),
		}, []string{"file"}),
	},
	{
		Name: "kern_surprising", Category: CategoryArch,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Surprising connections.",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"limit": strProp("Max connections to return (default 15)"),
		}, nil),
	},
	{
		Name: "kern_snapshot", Category: CategoryGraph,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Description:   "Versioned graph snapshot for cross-agent handoff.",
		InputSchema: schema(map[string]any{
			"action": strProp("create | verify (required)"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"symbol": strProp("Symbol for subgraph mode (create; omit for whole-repo mode)"),
			"limit":  strProp("Max graph symbols (create, whole-repo mode; default 400)"),
			"file":   strProp("Snapshot file path (verify)"),
		}, []string{"action"}),
	},
	{
		Name: "kern_cochange", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Co-change analysis: which files are actually changed together in the same commits, independent of the call graph.",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"range": strProp("Git range like 'HEAD~10..HEAD' (default last 30 commits)"),
			"limit": strProp("Max co-change pairs to return (default 20)"),
		}, nil),
	},
	{
		Name: "kern_usage_guide", Category: CategoryMeta,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "low",
		Description:   "Categorized usage guide for every kern MCP tool with performance tiers, recommended workflows, and pitfalls.",
		InputSchema:   schema(map[string]any{}, nil),
	},
	{
		Name: "kern_analyze", Category: CategoryAnalyze,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "medium",
		Description:   "Analyze a proposed change or symbol and return a review report under a lens (security, performance, maintainability, architecture) with the persisted task ID.",
		InputSchema: schema(map[string]any{
			"root":    strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"change":  strProp("The change/symbol to analyze, e.g. 'Add a Greet function' or 'helper'"),
			"lens":    strProp("Review lens: security, performance, maintainability, architecture, balanced, or a combined preset."),
			"profile": strProp("Output profile: machine-json, action-first, human-readable, or debug (default human-readable)."),
		}, []string{"change"}),
	},
	{
		Name: "kern_plan", Category: CategoryAnalyze,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "medium",
		Description:   "Produce an implementation plan for a proposed change: affected files, dependencies, risks, required validation. Deterministic; no LLM.",
		InputSchema: schema(map[string]any{
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"change": strProp("The change to plan, e.g. 'Add a Greet function to main.go'"),
		}, []string{"change"}),
	},
	{
		Name: "kern_execute", Category: CategoryExec,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "critical",
		Description:   "Execute a change in an isolated sandbox worktree (autonomy L2).",
		InputSchema: schema(map[string]any{
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"patch": strProp("A unified diff to apply to the sandbox worktree"),
		}, []string{"patch"}),
	},
	{
		Name: "kern_verify", Category: CategoryVerify,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "medium",
		Description:   "Verify a change with the unified engine (build, unit tests, security, architecture, dependency).",
		InputSchema: schema(map[string]any{
			"root":    strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"types":   strProp("Comma-separated checks: build,test,security,architecture,dependency,cve,license,secrets (default 'build')"),
			"cve":     map[string]any{"type": "boolean", "description": "When true, run the govulncheck vulnerability scan (SKIPPED when govulncheck is not installed)"},
			"license": map[string]any{"type": "boolean", "description": "When true, classify module licenses from go.mod/vendor (deterministic, no network)"},
			"secrets": map[string]any{"type": "boolean", "description": "When true, scan git history for committed secrets (masked snippets only)"},
		}, nil),
	},
	{
		Name: "kern_incident", Category: CategoryIncident,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Investigate a production incident end-to-end: correlate the alert to service/evidence, derive root cause, summarize. correlate=true runs incident→twin→code.",
		InputSchema: schema(map[string]any{
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"alert":          strProp("JSON of a domain.Alert: {id,severity,message,service,source,occurred_at}"),
			"snapshot":       strProp("Optional JSON of a runtime snapshot: {events,deployments,commits}"),
			"correlate":      strProp("If true, run the incident→twin→code correlation engine and render its report instead of the full pipeline"),
			"runbook":        strProp("JSON of a heal playbook to add: {signature, steps}; signature = the incident's deterministic error signature"),
			"list_playbooks": strProp("If true, list the stored heal playbooks"),
		}, []string{"alert"}),
	},
	{
		Name: "kern_flight", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Replay the AI flight recorder for one task.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"task": strProp("Task id whose flight trail to replay"),
		}, []string{"task"}),
	},
	{
		Name: "kern_what_if", Category: CategoryAnalyze,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "medium",
		Description:   "Simulate the impact of a hypothetical change on the knowledge graph.",
		InputSchema: schema(map[string]any{
			"root":       strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"change":     strProp("The symbol to change/remove (qualified name), e.g. 'helper'"),
			"kind":       enumProp("Change kind to simulate (whatif.ChangeKind); defaults to remove_symbol", "remove_symbol", "change_dependency", "add_symbol", "change_signature", "add_dependency", "remove_dependency", "rename_symbol", "split_service", "move_module", "change_infra"),
			"new_target": strProp("For change_dependency: the symbol Target now depends on"),
		}, []string{"change"}),
	},
	{
		Name: "kern_impact", Category: CategoryAnalyze,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "medium",
		Description:   "Estimate the impact/blast radius of a change to a symbol.",
		InputSchema: schema(map[string]any{
			"root":       strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"change":     strProp("The symbol to change/remove (qualified name), e.g. 'helper'"),
			"kind":       enumProp("Change kind to simulate (whatif.ChangeKind); defaults to remove_symbol", "remove_symbol", "change_dependency", "add_symbol", "change_signature", "add_dependency", "remove_dependency", "rename_symbol", "split_service", "move_module", "change_infra"),
			"new_target": strProp("For change_dependency: the symbol Target now depends on"),
			"risk":       strProp("When true, render the governance risk assessment (kern_risk contract) instead of the impact report"),
		}, []string{"change"}),
	},
	{
		Name: "kern_agents", Category: CategoryAgent,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Build the standard specialist team and list its roster (name, role, capabilities) plus current task states.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_loop", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "high",
		Description:   "Run the closed autonomy loop on an intent; returns the stage timeline and outcome. observe: deterministic handlers, L0-L5 gating; autonomous: LLM coder/planner.",
		InputSchema: schema(map[string]any{
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"intent": strProp("The intent/goal to run the loop against"),
			"level":  strProp("Autonomy level L0-L5 (observe default L0 read-only; autonomous default L2 sandboxed code changes)"),
			"mode":   strProp("Loop mode: 'observe' (default, deterministic) or 'autonomous' (LLM coder + planner with pre-flight probe)"),
		}, []string{"intent"}),
	},
	{
		Name: "kern_run", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "high",
		Description:   "Run an intent through the full task pipeline.",
		InputSchema: schema(map[string]any{
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"intent": strProp("The intent/goal to run through the pipeline"),
		}, []string{"intent"}),
	},
	{
		Name: "kern_meta", Category: CategoryMeta,
		SchemaVersion: SchemaVersionV1,
		Phase:         "meta",
		RiskLevel:     "medium",
		Cacheable:     true,
		Description:   "Single entry point: describe what you need in natural language; kern classifies and runs the right tool(s). Full catalog reachable through this router.",
		InputSchema: schema(map[string]any{
			"request":  strProp("Natural-language request describing what you need from kern"),
			"phase":    enumProp("Agent phase hint for routing; the advertised tool list is filtered server-wide via KERN_MCP_PHASE. Optional.", "explore", "plan", "edit", "verify"),
			"agent_id": strProp("Agent identity for governed mode (P1.2): scopes results to what this agent may read; omit for raw mode."),
			"task":     strProp("Task ID for governed mode; pairs with agent_id to scope authorization to the task paths."),
			"scope":    map[string]any{"type": "object", "description": "Optional task scope object {paths, denied_paths, services, envs, artifacts} for governed mode."},
			"root":     strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"request"}),
	},
	{
		Name: "kern_workflow", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "high",
		Description:   "Coordinate the agent team for an intent (analyze → plan → approval → code → verify → pr); parks at the approval gate; resume with the same task_id.",
		InputSchema: schema(map[string]any{
			"root":    strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"intent":  strProp("The intent/goal to run through the agent team"),
			"task_id": strProp("Resume an approval-parked run for this task (omit to start a new run)"),
		}, []string{"intent"}),
	},
	{
		Name: "kern_onboard", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Session-start onboarding: register the repo, build/refresh the index, write AGENTS.md if missing.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_audit", Category: CategoryGovernance,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Description:   "Return the tamper-evident governance audit log for the project (every firewall decision/approval).",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_approve", Category: CategoryGovernance,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "medium",
		Description:   "Resolve a governance approval gate: no id lists pending approvals.",
		InputSchema: schema(map[string]any{
			"root":     strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"id":       strProp("Approval ID to approve/reject (omit to list pending approvals)"),
			"reject":   strProp("If true, reject the approval instead of approving it (default false)"),
			"reason":   strProp("Optional reason for the decision"),
			"approver": strProp("Optional approver identity (defaults to 'mcp-user')"),
		}, nil),
	},
	{
		Name: "kern_correlate", Category: CategoryIncident,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Correlate a production alert against the runtime into an evidence chain (alert→service→deployment→commit→symbol). code=true adds incident→twin→code.",
		InputSchema: schema(map[string]any{
			"root":     strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"alert":    strProp("JSON of a domain.Alert: {id,severity,message,service,source,occurred_at}"),
			"snapshot": strProp("Optional JSON of a runtime snapshot: {events,deployments,commits}"),
			"code":     strProp("If true, include the incident→twin→code correlation section in the report"),
		}, []string{"alert"}),
	},
	{
		Name: "kern_learn", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Extract recurring patterns from engineering memory above a threshold.",
		InputSchema: schema(map[string]any{
			"root":      strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"threshold": strProp("Minimum pattern count to surface (default 3)"),
		}, nil),
	},
	{
		Name: "kern_modernize", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Analyze the monolith and produce a phased modernization plan.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_health", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Real-time health snapshot of the kern MCP server.",
		InputSchema: schema(map[string]any{
			"root": strProp("Project root to check index freshness for (defaults to workspace root)"),
		}, nil),
	},
	{
		Name: "kern_compose", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "high",
		Description:   "Execute an ordered pipeline of kern tools in a single RPC round-trip, passing outputs between steps via $variable.",
		InputSchema: schema(map[string]any{
			"pipeline": map[string]any{
				"type":        "array",
				"description": "List of pipeline steps: [{tool: 'tool_name', args: {...}, bind: 'var_name', on_error: 'stop|skip|continue'}]",
				"items": map[string]any{
					"type": "object",
					"properties": map[string]any{
						"tool":     strProp("Tool name to execute, e.g. kern_search, kern_impact, kern_context"),
						"args":     map[string]any{"type": "object", "description": "Arguments to pass to the tool; supports $varname interpolation"},
						"bind":     strProp("Optional variable name to store the tool output in for downstream steps"),
						"on_error": strProp("Behavior on error: 'stop' (default), 'skip', or 'continue'"),
					},
					"required": []string{"tool"},
				},
			},
			"timeout": strProp("Per-step timeout in seconds (default 60)"),
		}, []string{"pipeline"}),
	},
	{
		Name: "kern_pre_edit", Category: CategoryEdit,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "medium",
		Description:   "Predict blast radius, callers, untested dependencies and boundary risks of modifying a file or symbol before you edit.",
		InputSchema: schema(map[string]any{
			"file":   strProp("Relative or absolute path to the file to be edited"),
			"lines":  strProp("Optional line or line range being edited, e.g. '45-90' or '120'"),
			"symbol": strProp("Optional specific symbol name targeted for modification"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_repair_diagnostics", Category: CategoryEdit,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "medium",
		Description:   "Compiler-error-to-AST auto-repair.",
		InputSchema: schema(map[string]any{
			"root":            strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"compiler_output": strProp("Raw output or error string from the compiler/linter (e.g. go build / go test)"),
			"apply":           strProp("'true' to apply the repairs directly to files, 'false' (default) to preview changes"),
		}, []string{"compiler_output"}),
	},
	{
		Name: "kern_refactor_transaction", Category: CategoryEdit,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "high",
		Description:   "Multi-file transactional AST refactoring: evaluate batch edits in a sandbox worktree with compile verification.",
		InputSchema: schema(map[string]any{
			"root":            strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"edits":           map[string]any{"type": "array", "items": map[string]any{"type": "object", "properties": map[string]any{"path": strProp("File path"), "content": strProp("New complete file content")}, "required": []string{"path", "content"}}, "description": "Array of file modifications to test and apply atomically"},
			"compile_command": strProp("Optional compilation command (default auto-detects go build ./...)"),
			"apply":           strProp("'true' commits to the live tree on success, 'false' (default) dry-run preview"),
			"format":          strProp("'text' (default) or 'json'"),
		}, []string{"edits"}),
	},
	{
		Name: "kern_prompt_fill", Category: CategoryEdit,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Render standardized, token-efficient agent prompts with auto-injected project layout and memory lessons.",
		InputSchema: schema(map[string]any{
			"template":      strProp("Template name to fill, e.g. 'debug', 'code-review', 'fix-bug', 'explain', 'write-tests', 'onboard'"),
			"task":          strProp("Task or error description to inject"),
			"file":          strProp("Optional target file path to inject into the template"),
			"slots":         map[string]any{"type": "object", "description": "Optional custom key-value slot overrides: keys match the template's {{KEY}} placeholders, values are their replacement strings"},
			"inject_memory": strProp("Whether to auto-inject relevant lessons from project brain (default true if task provided)"),
			"root":          strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"template"}),
	},
	{
		Name: "kern_semantic_diff", Category: CategoryEdit,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Functional AST-level symbol diff.",
		InputSchema: schema(map[string]any{
			"from":  strProp("Starting git revision (defaults to HEAD)"),
			"to":    strProp("Ending git revision or leave empty for working tree"),
			"range": strProp("Optional git range, e.g. 'HEAD~1..HEAD'"),
			"root":  strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_evidence_anchor", Category: CategoryEvidence,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "medium",
		Description:   "Validate code claims/citations (symbol, file:line), correct line drift, and generate a tamper-evident SHA-256 certificate.",
		InputSchema: schema(map[string]any{
			"claim":  strProp("Claim or citation to verify, e.g. 'internal/index/engine.go:45' or 'NewServer'"),
			"file":   strProp("Optional file path of the cited reference"),
			"line":   strProp("Optional 1-based line number of the cited reference"),
			"symbol": strProp("Optional symbol name of the cited reference"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_context_watch", Category: CategoryContext,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Monitor and audit rolling agent context.",
		InputSchema: schema(map[string]any{
			"text":   strProp("Active conversation context or candidate tool output to audit"),
			"budget": strProp("Session token budget limit (default 32000)"),
			"format": strProp("Output format: 'text' (default) or 'json'"),
		}, []string{"text"}),
	},
	{
		Name: "kern_agent_fingerprint", Category: CategoryAgent,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Hash and evaluate an agent's tool-call pattern from the audit trail to detect repetitive loops or behavioral drift.",
		InputSchema: schema(map[string]any{
			"agent_id": strProp("Agent identifier to analyze (defaults to current agent)"),
			"format":   strProp("Output format: 'text' (default) or 'json'"),
		}, nil),
	},
	{
		Name: "kern_explain", Category: CategoryReview,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Cacheable:     true,
		Description:   "Synthesize an end-to-end architectural narrative for a symbol or file: purpose, callers, callees, interfaces, testing posture.",
		InputSchema: schema(map[string]any{
			"target": strProp("Target symbol name or relative file path to explain"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"target"}),
	},
	{
		Name: "kern_cross_repo_impact", Category: CategoryAnalyze,
		SchemaVersion: SchemaVersionV1,
		Phase:         "plan",
		RiskLevel:     "medium",
		Description:   "Evaluate multi-repository blast radius.",
		InputSchema: schema(map[string]any{
			"target_symbol": strProp("Exported symbol or interface undergoing changes"),
			"linked_repos":  map[string]any{"type": "array", "items": map[string]any{"type": "string"}, "description": "Array of paths to linked repositories or microservices to scan"},
			"root":          strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"target_symbol"}),
	},
	{
		Name: "kern_memory_ranked", Category: CategoryMemory,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Retrieve past project lessons weighted by keyword relevance and time decay (half-life).",
		InputSchema: schema(map[string]any{
			"prompt":         strProp("Current task prompt or error context to find lessons for"),
			"k":              strProp("Maximum number of ranked lessons to return (default 5)"),
			"half_life_days": strProp("Decay half-life in days (default 7.0)"),
			"root":           strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"prompt"}),
	},
	{
		Name: "kern_policy_dsl", Category: CategoryGovernance,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "low",
		Description:   "Evaluate diffs, changed files and imports against policy-as-code rules (banned packages, protected paths, max diff.",
		InputSchema: schema(map[string]any{
			"policy":  strProp("Optional inline YAML/JSON policy rules or path to policy file"),
			"files":   map[string]any{"type": "array", "items": map[string]any{"type": "string"}, "description": "List of files being changed or checked"},
			"diff":    strProp("Optional unified git diff to scan for banned patterns/imports"),
			"imports": map[string]any{"type": "array", "items": map[string]any{"type": "string"}, "description": "Optional list of package imports to evaluate"},
			"root":    strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_agent_coordination", Category: CategoryAgent,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Workspace coordination protocol for multi-agent teams.",
		InputSchema: schema(map[string]any{
			"action":      strProp("Action to perform: 'handoff', 'claim', 'release', 'inbox', 'status'"),
			"agent_id":    strProp("Calling agent identifier"),
			"from_agent":  strProp("Source agent ID for handoff"),
			"to_agent":    strProp("Destination agent ID (or '*' for broadcast)"),
			"task_id":     strProp("Task identifier"),
			"resource":    strProp("Resource name to claim or release"),
			"ttl_seconds": strProp("Time-to-live in seconds for resource claims (default 300)"),
			"notes":       strProp("Handoff notes or completion description"),
			"payload":     map[string]any{"type": "object", "description": "Structured state payload passed in handoff — free-form JSON object; keys and shape are defined by the sending/receiving agents"},
			"root":        strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_agent_role_rbac", Category: CategoryAgent,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "high",
		Description:   "Identity-based role access control (RBAC): restrict sensitive tools (exec, delete, fix) by agent roles.",
		InputSchema: schema(map[string]any{
			"action":   strProp("Action: 'evaluate' (default), 'roles', 'assign', 'check'"),
			"agent_id": strProp("Agent identifier"),
			"role":     strProp("Role name (e.g. 'admin', 'architect', 'developer', 'junior_dev', 'reviewer', 'auditor')"),
			"tool":     strProp("Tool name being requested to evaluate"),
			"root":     strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_stream", Category: CategoryTask,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Inspect streaming status, partition large responses into token-friendly chunks, manage progress notification.",
		InputSchema: schema(map[string]any{
			"action":         strProp("Action: 'status' (default), 'chunk', 'channels', 'emit'"),
			"channel":        strProp("Stream channel name"),
			"payload":        strProp("Payload string to partition into chunks"),
			"chunk_size":     strProp("Maximum character size per chunk (default 1000)"),
			"progress_token": strProp("Client-supplied progress token for notification dispatch"),
			"percent":        strProp("Progress percentage (0-100)"),
			"message":        strProp("Progress message text"),
		}, nil),
	},
	{
		Name: "kern_ast_transform", Category: CategoryAST,
		Slow:          true,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "high",
		Description:   "Deterministic AST-level transformations: scaffold interface method stubs, add struct fields, insert methods.",
		InputSchema: schema(map[string]any{
			"action":           strProp("Action: 'implement_interface' (default), 'add_field', 'add_method'"),
			"file":             strProp("Path to target source file"),
			"code":             strProp("Raw code snippet if file is not provided"),
			"target_symbol":    strProp("Target struct or type name (e.g. 'Server')"),
			"interface_name":   strProp("Interface to implement (e.g. 'io.Reader', 'http.Handler')"),
			"receiver_name":    strProp("Receiver variable name (e.g. 's')"),
			"receiver_type":    strProp("Receiver type name (e.g. '*Server')"),
			"field_name":       strProp("For add_field: field name"),
			"field_type":       strProp("For add_field: field type"),
			"field_tag":        strProp("For add_field: struct tag"),
			"method_signature": strProp("For add_method: method signature"),
			"method_body":      strProp("For add_method: method body"),
			"apply":            strProp("If true, writes changes directly to file"),
			"format":           strProp("Output format: 'text' (default) or 'json'"),
			"root":             strProp("Project root directory"),
		}, nil),
	},
	{
		Name: "kern_semantic_merge", Category: CategoryEdit,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "high",
		Description:   "AST-aware 3-way code merge (base/local/remote): resolves non-overlapping struct fields, methods, imports.",
		InputSchema: schema(map[string]any{
			"file":        strProp("Target file path"),
			"base":        strProp("Base version code or file path"),
			"local":       strProp("Local version code or file path"),
			"remote":      strProp("Remote version code or file path"),
			"base_file":   strProp("Optional file path for base version"),
			"local_file":  strProp("Optional file path for local version"),
			"remote_file": strProp("Optional file path for remote version"),
			"apply":       strProp("If true and clean, writes merged result to target file (default false)"),
			"format":      strProp("Output format: 'text' (default) or 'json'"),
			"root":        strProp("Project root directory"),
		}, nil),
	},
	{
		Name: "kern_deploy", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "edit",
		RiskLevel:     "critical",
		Description:   "Deploy a task via TaskService.",
		InputSchema: schema(map[string]any{
			"task_id": strProp("Task ID to deploy"),
			"version": strProp("Optional version string (default empty)"),
			"root":    strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, []string{"task_id"}),
	},
	{
		Name: "kern_runtime", Category: CategoryProject,
		SchemaVersion: SchemaVersionV1,
		Phase:         "explore",
		RiskLevel:     "low",
		Description:   "Production snapshot: status reports the wired runtime source (KERN_PROMETHEUS_URL/KERN_OTEL_URL/KERN_K8S_API or .kern/runtime.json); drift compares routes.",
		InputSchema: schema(map[string]any{
			"action": strProp("'status' (default) or 'drift'"),
			"root":   strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_evidence", Category: CategoryEvidence,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "medium",
		Description:   "Signed-evidence read path: verify validates a bundle (file or url) — tamper-seal, signature, audit-chain replay.",
		InputSchema: schema(map[string]any{
			"action":             strProp("'verify' (default), 'explain', or 'export'"),
			"file":               strProp("Bundle JSON file to verify/explain"),
			"url":                strProp("Bundle URL to fetch and verify/explain without cloning"),
			"task_id":            strProp("Task ID to scope the export (default: current state, like the CLI)"),
			"expect_fingerprint": strProp("Require the bundle to be signed by this key fingerprint (the trust anchor)"),
			"root":               strProp("Project root (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_synthesize_test", Category: CategoryVerify,
		SchemaVersion: SchemaVersionV1,
		Phase:         "verify",
		RiskLevel:     "medium",
		Description:   "Synthesize table-driven unit tests for untested functions from AST signatures.",
		InputSchema: schema(map[string]any{
			"target":   strProp("Target function or method name (e.g. 'Compute', 'Worker.Process')"),
			"file":     strProp("Target source file path"),
			"code":     strProp("Raw source code snippet if file is not provided"),
			"auto_gap": strProp("If true and target is empty, picks top untested hotspot from index"),
			"apply":    strProp("If true, writes synthesized test to <file>_test.go directly"),
			"format":   strProp("Output format: 'text' (default) or 'json'"),
			"root":     strProp("Project root directory"),
			"sinks":    strProp("Comma-separated security sink rule ids to scaffold one deterministic test per tainted sink (e.g. sql-injection,command-injection)"),
		}, nil),
	},
	{
		Name: "kern_org_projects", Category: CategoryOrg,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Enterprise org admin: list registered projects (C11). Returns {projects:[{name,root}],count}.",
		InputSchema: schema(map[string]any{
			"root":     strProp("Project root for the default project (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"projects": strProp("Optional comma-separated NAME=PATH pairs registering multiple projects"),
		}, nil),
	},
	{
		Name: "kern_org_agents", Category: CategoryOrg,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Org admin: register or list agent identities (C11). action=list returns agents.",
		InputSchema: schema(map[string]any{
			"action":   strProp("'list' (default) or 'register'"),
			"id":       strProp("Agent ID (required for register)"),
			"name":     strProp("Agent display name (required for register)"),
			"type":     strProp("Agent type (default 'default')"),
			"root":     strProp("Project root for the default project (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"projects": strProp("Optional comma-separated NAME=PATH pairs registering multiple projects"),
		}, nil),
	},
	{
		Name: "kern_org_teams", Category: CategoryOrg,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Org admin: manage teams grouping agents and projects (C11). action=list|show|create|remove; create takes id/name.",
		InputSchema: schema(map[string]any{
			"action":   strProp("'list' (default), 'show', 'create', or 'remove'"),
			"id":       strProp("Team ID (required for show/create/remove)"),
			"name":     strProp("Team display name (required for create)"),
			"projects": strProp("For create: comma-separated project names the team owns; for other tools: NAME=PATH registration pairs"),
			"members":  strProp("For create: comma-separated agent IDs that are team members"),
			"root":     strProp("Project root for the default project (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
		}, nil),
	},
	{
		Name: "kern_org_memory", Category: CategoryOrg,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Org admin: org-level shared memory across all projects (C11). action=list returns memories.",
		InputSchema: schema(map[string]any{
			"action":   strProp("'list' (default) or 'add'"),
			"content":  strProp("Memory content (required for add)"),
			"type":     strProp("Optional memory type (e.g. lesson, decision, incident, constraint)"),
			"root":     strProp("Project root for the default project (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"projects": strProp("Optional comma-separated NAME=PATH pairs registering multiple projects"),
		}, nil),
	},
	{
		Name: "kern_org_tasks", Category: CategoryOrg,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Enterprise org admin: aggregate task visibility (C11). Returns {projects:{name:[{id,state,intent,type}]},total}.",
		InputSchema: schema(map[string]any{
			"root":     strProp("Project root for the default project (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"projects": strProp("Optional comma-separated NAME=PATH pairs registering multiple projects"),
		}, nil),
	},
	{
		Name: "kern_org_search", Category: CategoryOrg,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Enterprise org admin: cross-project symbol search (C11). Requires q; returns hits.",
		InputSchema: schema(map[string]any{
			"q":        strProp("Search query (required)"),
			"root":     strProp("Project root for the default project (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"projects": strProp("Optional comma-separated NAME=PATH pairs registering multiple projects"),
		}, []string{"q"}),
	},
	{
		Name: "kern_org_audit", Category: CategoryOrg,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "low",
		Description:   "Enterprise org admin: org-level audit log (C11). Returns {entries:[...],count} with AuditEntry's raw JSON field names.",
		InputSchema: schema(map[string]any{
			"root":     strProp("Project root for the default project (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"projects": strProp("Optional comma-separated NAME=PATH pairs registering multiple projects"),
		}, nil),
	},
	{
		Name: "kern_org_user", Category: CategoryOrg,
		SchemaVersion: SchemaVersionV1,
		Phase:         "cross",
		RiskLevel:     "medium",
		Description:   "Org-wide user management + RBAC: user-add, user-list, user-role, user-disable, user-audit. actor_id required.",
		InputSchema: schema(map[string]any{
			"action":   strProp("One of user-add|user-list|user-role|user-disable|user-audit (required)"),
			"user_id":  strProp("Target user ID (required for user-add, user-role, user-disable, user-audit)"),
			"role":     strProp("Role to assign (required for user-add and user-role; org-admin or a member role)"),
			"actor_id": strProp("The acting user's ID (required on every action; RBAC-checked against the user registry)"),
			"root":     strProp("Project root for the default project (default: server cwd; stdio MCP server: confined to the workspace root, widenable via KERN_MCP_ROOTS; web console/SDK: confined to that App's project root — KERN_MCP_ROOTS does not widen)"),
			"projects": strProp("Optional comma-separated NAME=PATH pairs registering multiple projects"),
		}, []string{"action", "actor_id"}),
	},
}
