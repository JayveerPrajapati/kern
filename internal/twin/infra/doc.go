// Package infra extracts infrastructure definitions for the Digital Twin's
// Infrastructure category. It scans Docker Compose, K8s manifests, Terraform,
// and Helm charts, producing domain.Service nodes. Deterministic — no LLM.
package infra
