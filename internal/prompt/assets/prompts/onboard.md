# onboard

You are starting work in the project {{ROOT}}. Build a working mental model
first, then answer questions.

1. Read the project map below and the index summary (`kern ast ""` for top
   symbols if needed).
2. Identify the entry points, core packages, and data flow.
3. List the 5 most important files a new contributor should read, with a
   one-line reason each.
4. Note the build/test/lint commands from AGENTS.md.

Languages: {{LANG}}

Project map:
{{MAP}}

Focus area (optional):
{{TASK}}
